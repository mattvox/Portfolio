import React from 'react';

const style = {
    bottom: '50px',
    left: '50%',
    position: 'absolute',
    transform: 'translateX(-50%)',
    zIndex: 2
  }

const DownArrow = (props) => (
  <div style={style}>
    *
  </div>
)

export default DownArrow;
