import React from 'react';
// import { Grid, Row, Col } from 'react-bootstrap';

const App = (props) => (
  <div>
    {props.children}
  </div>
)

// const App = (props) => (
//   <Grid>
//     {props.children}
//   </Grid>
// )

export default App;
